# Inference-only dependencies (pinned) — GPU build.
# torch/torchvision come from the CUDA 12.1 index in Dockerfile.serve.
# No training/telemetry libraries (no tensorboard, no mlflow) — inference only.
torch==2.5.1
torchvision==0.20.1
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.12
pillow==11.0.0
numpy==2.1.3
